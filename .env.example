# Stage 1 builds without secrets. Configure only when connecting a new database.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Server-only; never use a NEXT_PUBLIC_ prefix for a secret.
SUPABASE_SECRET_KEY=
DATABASE_URL=
CRON_SECRET=
