# 阶段 8：生产准备记录

准备验证时间：2026-09-16 12:32（北京时间）。

## 已完成

- 新增完整数据来源与口径页面 `/about-data`，覆盖 6 项 World Bank 指标代码、单位、缺失和比较规则。
- 新增公开健康检查 `/api/health`，仅返回服务、快照和范围状态，不暴露凭据。
- 新增生产准备脚本，检查 8 国、6 指标、48 组序列、有限数值、严格年度顺序和公开环境变量命名。
- 新增 GitHub Actions 月度同步工作流：每月 1 日北京时间 10:17 检查，也支持手动运行。
- 工作流未配置 `DATABASE_URL` 时安全跳过并给出警告，不尝试连接未知数据库。
- 新增 `DEPLOYMENT.md`，记录 Vercel 发布路径、Supabase 首次连接、迁移顺序、同步与修订保护。
- 本地生产准备检查通过；公开快照仍可独立运行。

## 验证证据

1. 26 项测试、lint、类型检查和正式构建通过。
2. 构建生成 16 个页面/接口，包括 `/about-data` 与 `/api/health`。
3. 数据说明页桌面文档宽 1265/1280，手机文档宽 360/375，无页面级横向溢出。
4. 数据说明页显示 6 张指标来源卡，浏览器 error/warn 日志为空。
5. 健康接口返回 HTTP 200、8 国、6 指标、48 组序列、来源更新时间和数据库配置状态。
6. 生产准备检查 `ready: true`，无失败项；唯一警告为 `DATABASE_URL` 尚未配置。

## 尚未完成

第 8 阶段尚未完成，不能标记为生产数据库已连接。还需要：

1. 用户在 Supabase 新建一个空项目。
2. 对该空项目执行两份版本化迁移。
3. 将连接串保存为 GitHub Actions 仓库密钥 `DATABASE_URL`。
4. 手动运行首次同步并核验 6 项指标、48 组覆盖和修订提案。
5. 重新运行生产准备检查并完成最终全站验收。

在这些步骤完成前，公网网站继续使用已验证的版本化 World Bank 快照；功能可正常访问，但数据库状态必须保持为“未配置”。
